name: Test

on: [push, pull_request]

jobs:
  entrypoint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Config layering self-check
        run: ./test-entrypoint.sh
