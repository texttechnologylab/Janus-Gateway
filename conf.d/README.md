# Override directory

Files placed here are copied over the image's stock Janus configs at startup.
The directory is empty by design - an empty override set runs stock Janus.

Only add the files you actually change. See
[Configuration](https://texttechnologylab.github.io/Janus-Gateway/configuration/).
